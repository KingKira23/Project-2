<div class="container-fluid">
    <div class="main">
        {{> header splash=true}}
<h1>Gallery</h1>
    <div>
        <div class="row" id="galleryContainer">
            
        </div>

        {{!-- <div class="row">
            {{#each art}}
            <div class="col-xl-4 col-m-6 col-xs-12">
                <div class="frame">
                    <img src="{{ artUrl }}" alt="{{ artTitle }}" data-id="{{id}}" class="art"></img>
                </div>
                <p>{{ artTitle }}</p>
                <p>{{ artist }}</p>
            </div>
            {{/each}}
        </div> --}}

    </div>
    </div>
</div>
<script src="/js/gallery.js"></script>